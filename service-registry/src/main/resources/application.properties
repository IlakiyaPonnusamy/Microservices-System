spring.application.name=service-registry

server.port = 8761

#it does not need to register itself as a client.
eureka.client.register-with-eureka=false 

#no need to fetch registry details from another Eureka server
eureka.client.fetch-registry=false
